{
  "name": "@mmorpg/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"server lint pending\"",
    "test": "echo \"server tests pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mmorpg/shared": "workspace:*",
    "drizzle-orm": "^0.43.1",
    "pg": "^8.16.0",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.3"
  }
}
